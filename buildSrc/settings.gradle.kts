rootProject.name="kolibrium-conventions"

pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
        id("de.fayard.refreshVersions") version "0.60.3"
////                                # available:"0.60.4"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}
